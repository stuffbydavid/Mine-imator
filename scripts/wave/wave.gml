/// wave(time, offset, speed, strength)
/// @arg time
/// @arg offset
/// @arg speed
/// @arg strength

var time, offset, spd, strength;
var xx;
time = argument[0]
offset = argument[1]
spd = argument[2]
strength = argument[3]

xx = (time + offset) * spd

