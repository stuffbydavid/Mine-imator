/// test(expression, true, false)
/// @arg expression
/// @arg true
/// @arg false
/// @desc Ternary operator is broken YYC, so this is used instead.

if (argument0)
	return argument1
else
	return argument2