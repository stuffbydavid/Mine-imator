/// type_is_shape(type)
/// @arg type

var type = argument0;

return (type = "cube" || 
        type = "sphere" || 
        type = "cone" || 
        type = "cylinder" || 
        type = "surface")
