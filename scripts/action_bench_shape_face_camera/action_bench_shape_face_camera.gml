/// action_bench_shape_face_camera(enable)
/// @arg enable

function action_bench_shape_face_camera(enable)
{
	bench_settings.shape_face_camera = enable
	bench_settings.preview.update = true
}
