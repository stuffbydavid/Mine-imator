/// type_is_timeline(type)
/// @arg type

var type = argument0;

return (type = "camera" || 
        type = "pointlight" || 
        type = "spotlight" || 
        type = "background" || 
        type = "folder" || 
        type = "audio")
