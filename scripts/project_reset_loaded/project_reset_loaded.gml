/// project_reset_loaded()

with (obj_template)
    loaded = false

with (obj_particle_type)
    loaded = false

with (obj_timeline)
    loaded = false

with (obj_resource)
    loaded = false

with (obj_keyframe)
    loaded = false
