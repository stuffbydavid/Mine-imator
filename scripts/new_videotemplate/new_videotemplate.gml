/// new_videotemplate(name, width, height)
/// @arg name
/// @arg width
/// @arg height

with (new(obj_videotemplate))
{
	name = argument0
	width = argument1
	height = argument2

	return id
}