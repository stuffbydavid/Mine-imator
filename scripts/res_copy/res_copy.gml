/// res_copy(to)
/// @arg to

var to = argument0;

to.iid = iid
to.type = type
to.filename = filename
to.display_name = display_name
to.is_skin = is_skin
to.item_sheet_size = array_copy_1d(item_sheet_size)